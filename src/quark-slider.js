(function($) {
    $.fn.quarkSlider = function(id, options) {
		var settings = $.extend({
		    type: "standard",
			scrollType: "thumbnail",
			dotBar: "circle",
			navArrow: "standard",
		}, options);
	}
})(jQuery);
